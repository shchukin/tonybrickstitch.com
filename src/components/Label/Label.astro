---
import './Label.css';

interface Props {
  as?: 'div' | 'span' | 'b' | 'label';
  for?: string;
}

const {
  as: Tag = 'div',
} = Astro.props;

---

<Tag class="label" {...Astro.props.for && { for: Astro.props.for }}>
    <slot />
</Tag>
