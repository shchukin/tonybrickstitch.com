---
import './Label.css';

interface Props {
  as?: 'div' | 'span' | 'b' | 'label';
  for?: string;
}

const {
  as: Tag = 'div',
} = Astro.props;

---

<Tag class="label" for={Astro.props.for ? Astro.props.for : ''}>
    <slot />
</Tag>
